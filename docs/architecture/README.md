# Architecture Notes

This folder captures architecture notes and decisions for the NKP ZeroTouch
Framework.

Start here:

- `../architecture.md` for the current framework overview.
- `../phases.md` for the deployment phase model.
- `../environment-types.md` for connected, proxied, and air-gapped behavior.
- `../dashboard.md` for the local console design.
- `../container-runner.md` for Docker-based execution.

Recommended future additions:

- `decisions/` for architecture decision records.
- `data-flow.md` for config, secrets, generated artifacts, and run-state flow.
- `deployment-boundaries.md` for host, container, and air-gapped runner boundaries.

